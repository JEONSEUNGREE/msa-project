package com.example.userservicemsa.user.repository;

import com.example.userservicemsa.user.entity.HistoryMs;
import org.springframework.data.jpa.repository.JpaRepository;

public interface HistoryMsRepository extends JpaRepository<HistoryMs, Long> {
}
