package com.example.userservicemsa.user.repository;

import com.example.userservicemsa.user.entity.AuthMs;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AuthMsRepository extends JpaRepository<AuthMs, Long> {
}
